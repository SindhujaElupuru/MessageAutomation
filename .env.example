# Webhook secret (set same value in GitHub webhook)
WEBHOOK_SECRET=your-webhook-secret

# Token used to post comments (for testing use a PAT; for production prefer a GitHub App)
GITHUB_TOKEN=ghp_xxx

# Port the service listens on
PORT=3000
